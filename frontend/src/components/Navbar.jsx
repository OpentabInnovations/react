import React from "react"
import "../CSS/navbar.css"

const Navbar = (props) => {
  return (
    <div>
      <div className="navbar">
        this is navbar
      </div>
    </div>
  )
};

export default Navbar;
